# SloughGPT Environment Configuration

# API Server
SLOUGHGPT_ENV=production
SLOUGHGPT_LOG_LEVEL=INFO
SLOUGHGPT_HOST=0.0.0.0
SLOUGHGPT_PORT=8000

# Auto-start feedback workflow on boot
SLOUGHGPT_AUTO_WORKFLOW=true

# Embedding provider (sentence_transformers, openai, etc.)
EMBEDDING_PROVIDER=sentence_transformers
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Vector store (optional)
PINECONE_API_KEY=
PINECONE_INDEX=sloughgpt
PINECONE_ENVIRONMENT=us-east-1

# GPU acceleration
CUDA_VISIBLE_DEVICES=0
