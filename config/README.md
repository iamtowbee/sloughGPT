# Configuration samples

| File | Purpose |
|------|---------|
| `datasets.yaml` | Example multi-dataset mixing layout (see standards docs; not loaded by default `cli.py train`) |
| `environment.yml` | Conda-style dependency sketch |
| `production.env.example` | Example env vars — copy/rename for deployment; **do not commit secrets** |

Authoritative training config for **`cli.py train`** remains **`config.yaml`** at the **repository root**.
