'use client'

/**
 * Stable import path for `@sloughgpt/strui` in the Next app (`@/components/strui`).
 * Primitives stay under `@/components/ui/*` shims; use this for composed layouts and AI blocks.
 */
export * from '@sloughgpt/strui'
