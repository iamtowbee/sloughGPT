import { Meta } from '@storybook/blocks'

<Meta title="Docs/Introduction" />

<div className="str-safe-x mx-auto max-w-3xl space-y-8 py-4 text-left">
  <header className="space-y-3 border-b border-border pb-8">
    <p className="text-xs font-bold uppercase tracking-[0.22em] text-primary">SloughGPT</p>
    <h1 className="sl-h1">strui design system</h1>
    <p className="text-base leading-relaxed text-muted-foreground">
      <strong className="font-medium text-foreground">strui</strong> (<code className="rounded-none bg-muted px-1.5 py-0.5 font-mono text-sm">@sloughgpt/strui</code>) is the shared UI layer for the SloughGPT web app: <strong>pastel lattice</strong> tokens (lavender / periwinkle / mint accents), sharp geometry (no radius), Radix primitives, and composed blocks for dashboards and AI surfaces.
    </p>
  </header>

  <section className="space-y-3">
    <h2 className="sl-h2">How to use this book</h2>
    <ul className="list-inside list-disc space-y-2 text-sm text-muted-foreground">
      <li>
        Use the <strong className="text-foreground">Surface</strong> toolbar (sun / moon) to flip <strong className="text-foreground">light</strong> and <strong className="text-foreground">dark</strong> — components use CSS variables from <code className="font-mono text-xs">globals.css</code>.
      </li>
      <li>
        <strong className="text-foreground">Docs → Foundations</strong> shows the color roles and typography scale.
      </li>
      <li>
        <strong className="text-foreground">Docs → Design principles</strong> summarizes accessibility, layout, responsive, motion, and content conventions.
      </li>
      <li>
        <strong className="text-foreground">Docs → Component gallery</strong> is a single scrollable overview of primitives, composed, and AI pieces.
      </li>
      <li>
        Each component story under <strong className="text-foreground">UI</strong>, <strong className="text-foreground">Composed</strong>, and <strong className="text-foreground">AI</strong> includes <strong className="text-foreground">Controls</strong> for props and <strong className="text-foreground">Docs</strong> for the API table.
      </li>
    </ul>
  </section>

  <section className="space-y-3 border-t border-border pt-8">
    <h2 className="sl-h2">Consume in apps</h2>
    <pre className="sl-code overflow-x-auto p-4 text-left text-xs">
      <code>{`import { Button, PageHeader, cn } from '@sloughgpt/strui'
import '@sloughgpt/strui/styles/globals.css'`}</code>
    </pre>
    <p className="text-sm text-muted-foreground">
      Next.js apps can re-export from thin <code className="font-mono text-xs">'use client'</code> shims and add strui to Tailwind <code className="font-mono text-xs">content</code> — see the repo README.
    </p>
  </section>
</div>
